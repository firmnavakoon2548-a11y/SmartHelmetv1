# คู่มือระบบส่งรหัสสั้นผ่านบลูทูธไปยัง ESP32 เพื่อเล่นเสียง MP3 ผ่าน DFPlayer Mini
> **ชื่อไฟล์คู่มือ:** `dfplayer.md`  
> **วัตถุประสงค์:** อธิบายการทำงานของรหัสสั้น (Short Code เช่น รหัส `A`, `B`, `C`, `TL`, `TR`, `M50`) ที่ส่งจากแอปพลิเคชันผ่านสัญญาณบลูทูธ (Web Bluetooth BLE) ไปยังบอร์ด ESP32 บนหมวกอัจฉริยะ เพื่อให้ ESP32 ตรวจสอบรหัสแล้วสั่งโมดูล **DFPlayer Mini** ดึงไฟล์เสียงตัวเลข `.mp3` จาก MicroSD Card มาเล่นออกลำโพงของหมวก

---

## 1. หลักการทำงานของระบบ (System Architecture)

```text
[ สมาร์ตโฟน / เว็บแอป ]
       │
       │ (1) ตรวจพบเหตุการณ์นำทาง / ผู้พิการก้าวเดิน
       │ (2) แปลงข้อความเสียงเป็นรหัสสั้น เช่น "A"
       ▼
[ Web Bluetooth (BLE UART) ] 
       │
       │ (ส่งข้อความ String "A\n" ผ่านสัญญาณบลูทูธไร้สาย)
       ▼
[ บอร์ด ESP32 บนหมวกอัจฉริยะ ]
       │
       │ (3) อ่านค่าบลูทูธ ตรวจจับได้รหัส "A"
       │ (4) แมปปิ้งรหัส "A" ➜ ไฟล์เสียงหมายเลข 1 (0001.mp3)
       │ (5) ส่งคำสั่ง UART: myDFPlayer.playMp3Folder(1);
       ▼
[ โมดูล DFPlayer Mini + MicroSD Card ]
       │
       │ (6) ดึงไฟล์ /mp3/0001.mp3 ขยายสัญญาณเสียง
       ▼
[ ลำโพงที่ติดอยู่บนหมวกอัจฉริยะ (Speaker 8Ω 3W) ]
       🔊 "เริ่มต้นการนำทางค่ะ กรุณาเดินตามคำแนะนำเสียง"
```

---

## 2. ตารางรหัสสั้นบลูทูธและชื่อไฟล์ตัวเลข MP3 (DFPlayer Mini Code Table)

โมดูล DFPlayer Mini จะอ่านไฟล์จากโฟลเดอร์ชื่อ `/mp3/` บน MicroSD Card โดยตั้งชื่อไฟล์เป็นตัวเลข 4 หลัก เช่น `0001.mp3`, `0002.mp3`

### หมวดที่ 1: สถานะการนำทางและจุดเริ่มต้น/สิ้นสุด (Navigation Lifecycle)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`A`** | `0001.mp3` | 1 | เริ่มต้นการนำทางค่ะ กรุณาเดินตามคำแนะนำเสียง | กดปุ่มเริ่มนำทางเส้นทางขาไป |
| **`B`** | `0002.mp3` | 2 | เริ่มต้นการนำทางขากลับค่ะ ระบบได้สลับเส้นทางย้อนกลับให้อัตโนมัติ | ระบบตรวจพบว่าอยู่ใกล้จุดปลายทางและกดเริ่มเดินกลับ |
| **`C`** | `0003.mp3` | 3 | หยุดการนำทางแล้วค่ะ | กดปุ่มหยุดการนำทาง |
| **`D`** | `0004.mp3` | 4 | ยินดีด้วยค่ะ คุณเดินทางถึงจุดหมายปลายทางเรียบร้อยแล้วค่ะ | ผู้ใช้เดินถึงจุดหมายปลายทางสุดท้าย (ระยะ 0 เมตร) |
| **`E`** | `0005.mp3` | 5 | ปลายทางอยู่ไกลเกิน 10 กิโลเมตร ระบบได้ยกเลิกการนำทางอัตโนมัติเพื่อความปลอดภัยค่ะ | เลือกระยะทางเกิน 10 กม. ระบบยกเลิกเพื่อความปลอดภัย |
| **`F`** | `0006.mp3` | 6 | กำลังคำนวณเส้นทางเดินเท้า กรุณารอสักครู่ค่ะ | ขณะแอปกำลังดึงแนวทางเท้า OSRM และคำนวณพิกัด |
| **`G`** | `0007.mp3` | 7 | เลือกเส้นทางเรียบร้อยแล้วค่ะ กดเริ่มการนำทางได้เลยค่ะ | เมื่อเลือกเส้นทางจากคลังบันทึกพร้อมเริ่มต้นเดิน |
| **`H`** | `0008.mp3` | 8 | ยังไม่มีเส้นทางที่กำลังนำทางค่ะ กรุณาเลือกจุดหมายก่อน | กดปุ่มทวนเสียงแต่ยังไม่ได้เริ่มการนำทาง |

---

### หมวดที่ 2: ระยะทางคงเหลือเข้าสู่จุดเลี้ยว (Distance Milestones)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`M1000`** | `0010.mp3` | 10 | อีก 1 กิโลเมตร | แจ้งเตือนระยะทางคงเหลือ 1,000 เมตร |
| **`M500`** | `0011.mp3` | 11 | อีก 500 เมตร | แจ้งเตือนระยะทางคงเหลือ 500 เมตร |
| **`M300`** | `0012.mp3` | 12 | อีก 300 เมตร | แจ้งเตือนระยะทางคงเหลือ 300 เมตร |
| **`M200`** | `0013.mp3` | 13 | อีก 200 เมตร | แจ้งเตือนระยะทางคงเหลือ 200 เมตร |
| **`M150`** | `0014.mp3` | 14 | อีก 150 เมตร | แจ้งเตือนระยะทางคงเหลือ 150 เมตร |
| **`M100`** | `0015.mp3` | 15 | อีก 100 เมตร | แจ้งเตือนระยะทางคงเหลือ 100 เมตร |
| **`M90`** | `0016.mp3` | 16 | อีก 90 เมตร | แจ้งเตือนระยะทางคงเหลือ 90 เมตร |
| **`M80`** | `0017.mp3` | 17 | อีก 80 เมตร | แจ้งเตือนระยะทางคงเหลือ 80 เมตร |
| **`M70`** | `0018.mp3` | 18 | อีก 70 เมตร | แจ้งเตือนระยะทางคงเหลือ 70 เมตร |
| **`M60`** | `0019.mp3` | 19 | อีก 60 เมตร | แจ้งเตือนระยะทางคงเหลือ 60 เมตร |
| **`M50`** | `0020.mp3` | 20 | อีก 50 เมตร | แจ้งเตือนระยะทางคงเหลือ 50 เมตร |
| **`M40`** | `0021.mp3` | 21 | อีก 40 เมตร | แจ้งเตือนระยะทางคงเหลือ 40 เมตร |
| **`M30`** | `0022.mp3` | 22 | อีก 30 เมตร | แจ้งเตือนระยะทางคงเหลือ 30 เมตร |
| **`M25`** | `0023.mp3` | 23 | อีก 25 เมตร | แจ้งเตือนระยะทางคงเหลือ 25 เมตร |
| **`M20`** | `0024.mp3` | 24 | อีก 20 เมตร | แจ้งเตือนระยะทางคงเหลือ 20 เมตร |
| **`M15`** | `0025.mp3` | 25 | อีก 15 เมตร | แจ้งเตือนระยะประชิด 15 เมตร (เตรียมเลี้ยว) |
| **`M10`** | `0026.mp3` | 26 | อีก 10 เมตร | แจ้งเตือนระยะประชิด 10 เมตร (ชะลอความเร็ว) |
| **`M5`** | `0027.mp3` | 27 | อีก 5 เมตร | แจ้งเตือนระยะประชิด 5 เมตร (เตรียมเปลี่ยนทิศทางทันที) |
| **`M0`** | `0028.mp3` | 28 | อีก 0 เมตร ถึงจุดเลี้ยวแล้วค่ะ | ผู้ใช้ก้าวถึงตำแหน่งจุดเลี้ยวพอดี |

---

### หมวดที่ 3: ทิศทางการเลี้ยวและการเคลื่อนที่ (Turn Maneuvers & Actions)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`TS`** | `0030.mp3` | 30 | เดินตรงไปข้างหน้า | คำสั่งให้เดินตรงต่อไปตามแนวทางเท้า |
| **`TNL`** | `0031.mp3` | 31 | ข้างหน้าเลี้ยวซ้าย | คำสั่งเตือนล่วงหน้าก่อนถึงจุดเลี้ยวซ้าย |
| **`TL`** | `0032.mp3` | 32 | เลี้ยวซ้าย | คำสั่งให้เลี้ยวซ้ายทันที ณ จุดเลี้ยว |
| **`TNR`** | `0033.mp3` | 33 | ข้างหน้าเลี้ยวขวา | คำสั่งเตือนล่วงหน้าก่อนถึงจุดเลี้ยวขวา |
| **`TR`** | `0034.mp3` | 34 | เลี้ยวขวา | คำสั่งให้เลี้ยวขวาทันที ณ จุดเลี้ยว |
| **`TSL`** | `0035.mp3` | 35 | เบี่ยงซ้ายเล็กน้อย โค้งตามทาง | ทางข้างหน้าเป็นทางโค้งซ้ายตามแนวถนน |
| **`TSR`** | `0036.mp3` | 36 | เบี่ยงขวาเล็กน้อย โค้งตามทาง | ทางข้างหน้าเป็นทางโค้งขวาตามแนวถนน |
| **`TSHL`** | `0037.mp3` | 37 | เลี้ยวซ้ายหักศอก | จุดเลี้ยวซ้ายมุมแคบหรือหักมุมชัน |
| **`TSHR`** | `0038.mp3` | 38 | เลี้ยวขวาหักศอก | จุดเลี้ยวขวามุมแคบหรือหักมุมชัน |
| **`TU`** | `0039.mp3` | 39 | กลับหลังหัน ยูเทิร์น | คำสั่งให้หมุนตัวกลับหลัง 180 องศา |
| **`TX`** | `0040.mp3` | 40 | ข้างหน้าเตรียมข้ามถนน | เตือนล่วงหน้าก่อนถึงจุดที่ต้องข้ามถนน |
| **`TC`** | `0041.mp3` | 41 | ข้างหน้าเป็นทางม้าลาย ระวังรถแล้วเดินข้ามทางม้าลายค่ะ | เตือนจุดข้ามทางม้าลาย |
| **`TB`** | `0042.mp3` | 42 | ข้างหน้ามีสะพานลอย เตรียมขึ้นสะพานลอยค่ะ | เตือนตำแหน่งบันไดขึ้นสะพานลอยคนข้าม |
| **`TD`** | `0043.mp3` | 43 | ลงสะพานลอยระวังบันไดค่ะ | เตือนตำแหน่งบันไดลงสะพานลอย |
| **`TRP`** | `0044.mp3` | 44 | ระวังทางลาดชันข้างหน้า | เตือนทางลาดขึ้น/ลงฟุตบาท |

---

### หมวดที่ 4: ทิศทางตามเข็มทิศ 8 ทิศ (Compass Headings)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`CN`** | `0050.mp3` | 50 | มุ่งหน้าทิศเหนือ | แจ้งทิศทางเข็มทิศ (มุม 337.5° – 22.5°) |
| **`CNE`** | `0051.mp3` | 51 | มุ่งหน้าทิศตะวันออกเฉียงเหนือ | แจ้งทิศทางเข็มทิศ (มุม 22.5° – 67.5°) |
| **`CE`** | `0052.mp3` | 52 | มุ่งหน้าทิศตะวันออก | แจ้งทิศทางเข็มทิศ (มุม 67.5° – 112.5°) |
| **`CSE`** | `0053.mp3` | 53 | มุ่งหน้าทิศตะวันออกเฉียงใต้ | แจ้งทิศทางเข็มทิศ (มุม 112.5° – 157.5°) |
| **`CS`** | `0054.mp3` | 54 | มุ่งหน้าทิศใต้ | แจ้งทิศทางเข็มทิศ (มุม 157.5° – 202.5°) |
| **`CSW`** | `0055.mp3` | 55 | มุ่งหน้าทิศตะวันตกเฉียงใต้ | แจ้งทิศทางเข็มทิศ (มุม 202.5° – 247.5°) |
| **`CW`** | `0056.mp3` | 56 | มุ่งหน้าทิศตะวันตก | แจ้งทิศทางเข็มทิศ (มุม 247.5° – 292.5°) |
| **`CNW`** | `0057.mp3` | 57 | มุ่งหน้าทิศตะวันตกเฉียงเหนือ | แจ้งทิศทางเข็มทิศ (มุม 292.5° – 337.5°) |

---

### หมวดที่ 5: การเตือนเดินผิดทางและการแก้ไขทิศทาง (Off-Route Correction)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`EU`** | `0060.mp3` | 60 | เตือนเดินผิดทาง ออกนอกเส้นทาง กรุณากลับหลังหันเพื่อกลับสู่เส้นทางค่ะ | ผู้ใช้เดินย้อนศร หรือหันหลังออกจากจุดหมาย |
| **`EL`** | `0061.mp3` | 61 | เตือนเดินผิดทาง ออกนอกเส้นทาง ข้างหน้ากรุณาเลี้ยวซ้ายเพื่อกลับสู่เส้นทางค่ะ | ผู้ใช้เบี่ยงออกทางขวาหลุดถนน ต้องเลี้ยวซ้ายกลับเข้าทาง |
| **`ER`** | `0062.mp3` | 62 | เตือนเดินผิดทาง ออกนอกเส้นทาง ข้างหน้ากรุณาเลี้ยวขวาเพื่อกลับสู่เส้นทางค่ะ | ผู้ใช้เบี่ยงออกทางซ้ายหลุดถนน ต้องเลี้ยวขวากลับเข้าทาง |
| **`ES`** | `0063.mp3` | 63 | เตือนเดินผิดทาง ออกนอกเส้นทาง กรุณาเดินตรงไปเพื่อกลับสู่เส้นทางค่ะ | หลุดจากแนวทางเท้า แต่ทิศทางยังมุ่งไปได้ |
| **`EOK`** | `0064.mp3` | 64 | กลับเข้าสู่เส้นทางแล้วค่ะ | เดินกลับเข้ามาอยู่ในแนวทางเดินที่ถูกต้อง |

---

### หมวดที่ 6: ตำแหน่งทางเท้าและแนวกันชนถนน (Pedestrian Sidewalk Corridor)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`SL`** | `0070.mp3` | 70 | เดินริมทางเท้าฝั่งซ้าย | กำลังเดินบนทางเท้าฝั่งซ้ายของถนนถูกต้อง |
| **`SR`** | `0071.mp3` | 71 | เดินริมทางเท้าฝั่งขวา | กำลังเดินบนทางเท้าฝั่งขวาของถนนถูกต้อง |
| **`SC`** | `0072.mp3` | 72 | เดินกึ่งกลางทางเดิน | เดินอยู่บริเวณกึ่งกลางแนวทางเดิน |
| **`SW`** | `0073.mp3` | 73 | ระวัง ชิดขอบถนนเกินไป กรุณาเบี่ยงเข้าทางเท้า | เริ่มเดินเฉียดเข้าไปในเลนรถวิ่ง |
| **`SOK`** | `0074.mp3` | 74 | กำลังเดินตามแนวทางเท้า ปลอดภัยค่ะ | เดินตรงแนวต่อเนื่องอย่างปลอดภัย |

---

### หมวดที่ 7: สิ่งกีดขวางจากเซนเซอร์หมวกอัจฉริยะ (Obstacle Alerts)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`OF`** | `0080.mp3` | 80 | ตรวจพบสิ่งกีดขวางด้านหน้า กรุณาหยุดหรือชะลอความเร็วค่ะ | เซนเซอร์หน้าพบวัตถุระยะ 0.8–1.5 ม. |
| **`OL`** | `0081.mp3` | 81 | มีสิ่งกีดขวางด้านซ้าย กรุณาเบี่ยงขวาเล็กน้อยค่ะ | เซนเซอร์ซ้ายพบเสา/สิ่งกีดขวาง |
| **`OR`** | `0082.mp3` | 82 | มีสิ่งกีดขวางด้านขวา กรุณาเบี่ยงซ้ายเล็กน้อยค่ะ | เซนเซอร์ขวาพบสิ่งกีดขวาง |
| **`OH`** | `0083.mp3` | 83 | ระวัง สิ่งกีดขวางระดับศีรษะหรือป้ายด้านบนค่ะ | ตรวจพบสิ่งกีดขวางระดับหัว (ป้าย/กันสาด) |
| **`OG`** | `0084.mp3` | 84 | ระวัง ทางต่างระดับ หลุม หรือท่อระบายน้ำด้านหน้าค่ะ | ตรวจพบพื้นยุบลงหรือท่อระบายน้ำ |
| **`OC`** | `0085.mp3` | 85 | ทางข้างหน้าโล่ง เดินต่อไปได้ค่ะ | สิ่งกีดขวางพ้นระยะแล้ว |

---

### หมวดที่ 8: สถานะระบบและการเชื่อมต่อฮาร์ดแวร์ (System & Hardware Status)

| รหัสบลูทูธ (Short Code) | ชื่อไฟล์ MP3 | หมายเลขเพลง | คำพูดเสียงแจ้งเตือน (Thai Voice) | ความหมายและสถานการณ์การทำงาน |
|:---:|:---:|:---:|:---|:---|
| **`HB`** | `0090.mp3` | 90 | เชื่อมต่อหมวกอัจฉริยะบลูทูธเรียบร้อยแล้วค่ะ | เชื่อมต่อ BLE กับบอร์ด ESP32 สำเร็จ |
| **`HD`** | `0091.mp3` | 91 | การเชื่อมต่อหมวกอัจฉริยะหลุด กรุณาตรวจสอบบลูทูธค่ะ | สัญญาณบลูทูธกับหมวกขาดหาย |
| **`HG`** | `0092.mp3` | 92 | สัญญาณจีพีเอสพร้อมนำทางแล้วค่ะ | รับค่าพิกัดดาวเทียมได้แม่นยำ |
| **`HW`** | `0093.mp3` | 93 | สัญญาณจีพีเอสอ่อน กำลังค้นหาสัญญาณดาวเทียมค่ะ | สัญญาณดาวเทียมคลาดเคลื่อนหรืออยู่ในอาคาร |
| **`HL`** | `0094.mp3` | 94 | แบตเตอรี่หมวกอัจฉริยะเหลือต่ำกว่า 20 เปอร์เซ็นต์ กรุณาชาร์จค่ะ | แบตเตอรี่หมวกใกล้หมด |
| **`HR`** | `0095.mp3` | 95 | เริ่มต้นบันทึกเส้นทางใหม่ค่ะ | เริ่มโหมดบันทึก Waypoint |
| **`HS`** | `0096.mp3` | 96 | บันทึกเส้นทางสำเร็จและจัดเก็บลงในระบบแล้วค่ะ | บันทึกเส้นทางเสร็จสิ้น |
| **`HWP`** | `0097.mp3` | 97 | บันทึกจุดเลี้ยวเรียบร้อยแล้วค่ะ | กดปุ่มปักหมุดจุดเลี้ยวระหว่างทาง |

---

## 3. การเตรียม MicroSD Card สำหรับ DFPlayer Mini

1. ทำการฟอร์แมต MicroSD Card ให้เป็นแบบ **FAT32** (ความจุที่แนะนำ 2GB – 32GB)
2. สร้างโฟลเดอร์ชื่อ **`mp3`** (ตัวพิมพ์เล็กหรือพิมพ์ใหญ่ก็ได้) ที่ Root ของการ์ด
3. นำไฟล์เสียง MP3 ใส่ลงในโฟลเดอร์ `/mp3/` โดยตั้งชื่อเป็นตัวเลข 4 หลัก ดังนี้:
   ```text
   MicroSD Card
   └── mp3/
       ├── 0001.mp3   <-- (รหัส A : เริ่มต้นการนำทาง)
       ├── 0002.mp3   <-- (รหัส B : เริ่มต้นการนำทางขากลับ)
       ├── 0003.mp3   <-- (รหัส C : หยุดการนำทาง)
       ├── 0004.mp3   <-- (รหัส D : เดินทางถึงจุดหมาย 0 เมตร)
       ├── 0010.mp3   <-- (รหัส M1000 : อีก 1 กิโลเมตร)
       ├── 0020.mp3   <-- (รหัส M50 : อีก 50 เมตร)
       ├── 0028.mp3   <-- (รหัส M0 : อีก 0 เมตร ถึงจุดเลี้ยว)
       ├── 0030.mp3   <-- (รหัส TS : เดินตรงไปข้างหน้า)
       ├── 0032.mp3   <-- (รหัส TL : เลี้ยวซ้าย)
       ├── 0034.mp3   <-- (รหัส TR : เลี้ยวขวา)
       ├── 0060.mp3   <-- (รหัส EU : เตือนเดินผิดทาง กลับหลังหัน)
       ├── 0080.mp3   <-- (รหัส OF : ตรวจพบสิ่งกีดขวางหน้า)
       └── 0090.mp3   <-- (รหัส HB : เชื่อมต่อบลูทูธเรียบร้อย)
   ```

---

## 4. แผนผังการต่อวงจร (Wiring Diagram: ESP32 + DFPlayer Mini)

| ขาของ DFPlayer Mini | ต่อไปยังขาของ ESP32 / อุปกรณ์ | หมายเหตุ |
|:---|:---|:---|
| **VCC** | ต่อเข้ากับไฟ **5V** (ขา VIN / 5V ของ ESP32) | เพื่อให้มีพลังงานเพียงพอสำหรับแอมป์เสียง |
| **GND** | ต่อเข้ากับขา **GND** ของ ESP32 | กราวด์ร่วม |
| **RX** | ต่อเข้ากับขา **GPIO 17 (TX2)** ของ ESP32 | **แนะนำให้ต่อตัวต้านทาน 1kΩ คั่น** เพื่อลดสัญญาณรบกวน |
| **TX** | ต่อเข้ากับขา **GPIO 16 (RX2)** ของ ESP32 | รับสถานะจาก DFPlayer |
| **SPK_1 (+)** | ต่อเข้าขั้วบวก (+) ของลำโพง 8Ω 3W | ต่อตรงเข้าลำโพงได้เลย |
| **SPK_2 (-)** | ต่อเข้าขั้วลบ (-) ของลำโพง 8Ω 3W | ต่อตรงเข้าลำโพงได้เลย |

---

## 5. ตัวอย่างโค้ด Arduino C++ สำหรับบอร์ด ESP32 (`esp32_smart_helmet.ino`)

คัดลอกโค้ดด้านล่างนี้ไปใส่ในโปรแกรม Arduino IDE และอัปโหลดลงบอร์ด ESP32:

```cpp
#include <BLEDevice.h>
#include <BLEServer.h>
#include <BLEUtils.h>
#include <BLE2902.h>
#include <DFRobotDFPlayerMini.h>

// กำหนดขา Serial2 ของ ESP32 สำหรับเชื่อมต่อ DFPlayer Mini
#define RXD2 16
#define TXD2 17

HardwareSerial myHardwareSerial(2);
DFRobotDFPlayerMini myDFPlayer;

// BLE UUIDs (ใช้ Nordic UART Service ตามมาตรฐานสากล)
#define SERVICE_UUID           "6e400001-b5a3-f393-e0a9-e50e24dcca9e"
#define CHARACTERISTIC_UUID_RX "6e400002-b5a3-f393-e0a9-e50e24dcca9e"
#define CHARACTERISTIC_UUID_TX "6e400003-b5a3-f393-e0a9-e50e24dcca9e"

BLEServer *pServer = NULL;
BLECharacteristic *pTxCharacteristic = NULL;
bool deviceConnected = false;

// ฟังก์ชันแมปรหัสสั้น ➜ เล่นไฟล์ MP3 ตามหมายเลข
void handleAudioCommand(String code) {
  code.trim();
  code.toUpperCase();
  Serial.print("📡 ได้รับรหัสบลูทูธ: ");
  Serial.println(code);

  int trackNumber = -1;

  // หมวดที่ 1: สถานะการนำทาง
  if (code == "A" || code == "A1") trackNumber = 1;        // 0001.mp3 เริ่มต้นการนำทาง
  else if (code == "B" || code == "A2") trackNumber = 2;   // 0002.mp3 เริ่มต้นนำทางขากลับ
  else if (code == "C" || code == "A3") trackNumber = 3;   // 0003.mp3 หยุดการนำทาง
  else if (code == "D" || code == "A4") trackNumber = 4;   // 0004.mp3 เดินทางถึงจุดหมาย
  else if (code == "E" || code == "A5") trackNumber = 5;   // 0005.mp3 ยกเลิกเกิน 10 กม.
  else if (code == "F" || code == "A6") trackNumber = 6;   // 0006.mp3 กำลังคำนวณเส้นทาง
  else if (code == "G" || code == "A7") trackNumber = 7;   // 0007.mp3 เลือกเส้นทางแล้ว
  else if (code == "H" || code == "A8") trackNumber = 8;   // 0008.mp3 ยังไม่มีเส้นทาง

  // หมวดที่ 2: ระยะทางคงเหลือ
  else if (code == "M1000") trackNumber = 10;              // 0010.mp3 อีก 1 กิโลเมตร
  else if (code == "M500")  trackNumber = 11;              // 0011.mp3 อีก 500 เมตร
  else if (code == "M300")  trackNumber = 12;              // 0012.mp3 อีก 300 เมตร
  else if (code == "M200")  trackNumber = 13;              // 0013.mp3 อีก 200 เมตร
  else if (code == "M150")  trackNumber = 14;              // 0014.mp3 อีก 150 เมตร
  else if (code == "M100")  trackNumber = 15;              // 0015.mp3 อีก 100 เมตร
  else if (code == "M90")   trackNumber = 16;              // 0016.mp3 อีก 90 เมตร
  else if (code == "M80")   trackNumber = 17;              // 0017.mp3 อีก 80 เมตร
  else if (code == "M70")   trackNumber = 18;              // 0018.mp3 อีก 70 เมตร
  else if (code == "M60")   trackNumber = 19;              // 0019.mp3 อีก 60 เมตร
  else if (code == "M50")   trackNumber = 20;              // 0020.mp3 อีก 50 เมตร
  else if (code == "M40")   trackNumber = 21;              // 0021.mp3 อีก 40 เมตร
  else if (code == "M30")   trackNumber = 22;              // 0022.mp3 อีก 30 เมตร
  else if (code == "M25")   trackNumber = 23;              // 0023.mp3 อีก 25 เมตร
  else if (code == "M20")   trackNumber = 24;              // 0024.mp3 อีก 20 เมตร
  else if (code == "M15")   trackNumber = 25;              // 0025.mp3 อีก 15 เมตร
  else if (code == "M10")   trackNumber = 26;              // 0026.mp3 อีก 10 เมตร
  else if (code == "M5")    trackNumber = 27;              // 0027.mp3 อีก 5 เมตร
  else if (code == "M0")    trackNumber = 28;              // 0028.mp3 อีก 0 เมตร ถึงจุดเลี้ยว

  // หมวดที่ 3: ทิศทางการเลี้ยว
  else if (code == "TS" || code == "S") trackNumber = 30;  // 0030.mp3 เดินตรงไปข้างหน้า
  else if (code == "TNL") trackNumber = 31;                // 0031.mp3 ข้างหน้าเลี้ยวซ้าย
  else if (code == "TL" || code == "L") trackNumber = 32;  // 0032.mp3 เลี้ยวซ้าย
  else if (code == "TNR") trackNumber = 33;                // 0033.mp3 ข้างหน้าเลี้ยวขวา
  else if (code == "TR" || code == "R") trackNumber = 34;  // 0034.mp3 เลี้ยวขวา
  else if (code == "TSL") trackNumber = 35;                // 0035.mp3 เบี่ยงซ้ายเล็กน้อย
  else if (code == "TSR") trackNumber = 36;                // 0036.mp3 เบี่ยงขวาเล็กน้อย
  else if (code == "TSHL") trackNumber = 37;               // 0037.mp3 เลี้ยวซ้ายหักศอก
  else if (code == "TSHR") trackNumber = 38;               // 0038.mp3 เลี้ยวขวาหักศอก
  else if (code == "TU" || code == "U") trackNumber = 39;  // 0039.mp3 กลับหลังหัน ยูเทิร์น
  else if (code == "TX")  trackNumber = 40;                // 0040.mp3 ข้ามถนน
  else if (code == "TC")  trackNumber = 41;                // 0041.mp3 ทางม้าลาย
  else if (code == "TB")  trackNumber = 42;                // 0042.mp3 สะพานลอย

  // หมวดที่ 4: เข็มทิศ 8 ทิศ
  else if (code == "CN")  trackNumber = 50;                // 0050.mp3 มุ่งหน้าทิศเหนือ
  else if (code == "CNE") trackNumber = 51;                // 0051.mp3 ทิศตะวันออกเฉียงเหนือ
  else if (code == "CE")  trackNumber = 52;                // 0052.mp3 ทิศตะวันออก
  else if (code == "CSE") trackNumber = 53;                // 0053.mp3 ทิศตะวันออกเฉียงใต้
  else if (code == "CS")  trackNumber = 54;                // 0054.mp3 ทิศใต้
  else if (code == "CSW") trackNumber = 55;                // 0055.mp3 ทิศตะวันตกเฉียงใต้
  else if (code == "CW")  trackNumber = 56;                // 0056.mp3 ทิศตะวันตก
  else if (code == "CNW") trackNumber = 57;                // 0057.mp3 ทิศตะวันตกเฉียงเหนือ

  // หมวดที่ 5: ออกนอกเส้นทาง
  else if (code == "EU")  trackNumber = 60;                // 0060.mp3 เตือนเดินผิดทาง กลับหลังหัน
  else if (code == "EL")  trackNumber = 61;                // 0061.mp3 ออกนอกเส้นทาง เลี้ยวซ้าย
  else if (code == "ER")  trackNumber = 62;                // 0062.mp3 ออกนอกเส้นทาง เลี้ยวขวา
  else if (code == "ES")  trackNumber = 63;                // 0063.mp3 ออกนอกเส้นทาง เดินตรงไป
  else if (code == "EOK") trackNumber = 64;                // 0064.mp3 กลับเข้าสู่เส้นทางแล้ว

  // หมวดที่ 6: ทางเท้าและขอบถนน
  else if (code == "SL")  trackNumber = 70;                // 0070.mp3 เดินริมทางเท้าซ้าย
  else if (code == "SR")  trackNumber = 71;                // 0071.mp3 เดินริมทางเท้าขวา
  else if (code == "SC")  trackNumber = 72;                // 0072.mp3 เดินกึ่งกลางทาง
  else if (code == "SW")  trackNumber = 73;                // 0073.mp3 ชิดขอบถนนเกินไป

  // หมวดที่ 7: สิ่งกีดขวาง
  else if (code == "OF")  trackNumber = 80;                // 0080.mp3 สิ่งกีดขวางด้านหน้า
  else if (code == "OL")  trackNumber = 81;                // 0081.mp3 สิ่งกีดขวางด้านซ้าย
  else if (code == "OR")  trackNumber = 82;                // 0082.mp3 สิ่งกีดขวางด้านขวา
  else if (code == "OH")  trackNumber = 83;                // 0083.mp3 สิ่งกีดขวางระดับหัว
  else if (code == "OG")  trackNumber = 84;                // 0084.mp3 ทางต่างระดับ/หลุม

  // หมวดที่ 8: สถานะระบบ
  else if (code == "HB")  trackNumber = 90;                // 0090.mp3 เชื่อมต่อบลูทูธสำเร็จ
  else if (code == "HD")  trackNumber = 91;                // 0091.mp3 การเชื่อมต่อหลุด
  else if (code == "HG")  trackNumber = 92;                // 0092.mp3 GPS พร้อมนำทาง
  else if (code == "HL")  trackNumber = 94;                // 0094.mp3 แบตเตอรี่ต่ำกว่า 20%

  // หากพบหมายเลขเพลง ให้สั่ง DFPlayer Mini เล่นไฟล์ในโฟลเดอร์ /mp3/
  if (trackNumber > 0) {
    Serial.printf("🔊 กำลังสั่งเล่นไฟล์ MP3 หมายเลข: %04d.mp3\n", trackNumber);
    myDFPlayer.playMp3Folder(trackNumber);
  } else {
    Serial.println("⚠️ ไม่พบรหัสเสียงที่ตรงกัน");
  }
}

// คลาสจัดการการเชื่อมต่อบลูทูธ
class MyServerCallbacks: public BLEServerCallbacks {
  void onConnect(BLEServer* pServer) {
    deviceConnected = true;
    Serial.println("✅ แอปพลิเคชันเชื่อมต่อบลูทูธเรียบร้อยแล้ว");
    myDFPlayer.playMp3Folder(90); // เล่น 0090.mp3 (เชื่อมต่อสำเร็จ)
  }

  void onDisconnect(BLEServer* pServer) {
    deviceConnected = false;
    Serial.println("❌ แอปพลิเคชันตัดการเชื่อมต่อบลูทูธ");
    pServer->getAdvertising()->start(); // เปิดกระจายสัญญาณใหม่รอต่อรอบถัดไป
  }
};

// คลาสรับข้อมูลคำสั่ง String จากมือถือ
class MyCallbacks: public BLECharacteristicCallbacks {
  void onWrite(BLECharacteristic *pCharacteristic) {
    String rxValue = pCharacteristic->getValue().c_str();
    if (rxValue.length() > 0) {
      handleAudioCommand(rxValue);
    }
  }
};

void setup() {
  Serial.begin(115200);
  delay(1000);
  Serial.println("=== เริ่มต้นระบบ SafeSight ESP32 Smart Helmet ===");

  // 1. เริ่มต้น Serial2 สำหรับ DFPlayer Mini
  myHardwareSerial.begin(9600, SERIAL_8N1, RXD2, TXD2);

  if (!myDFPlayer.begin(myHardwareSerial)) {
    Serial.println("❌ ไม่สามารถเชื่อมต่อกับ DFPlayer Mini ได้");
    Serial.println("กรุณาตรวจสอบสายไฟ RX/TX และ MicroSD Card (FAT32)");
  } else {
    Serial.println("✅ DFPlayer Mini พร้อมทำงานแล้ว");
    myDFPlayer.volume(25); // ปรับระดับเสียง (0 - 30)
    delay(500);
    myDFPlayer.playMp3Folder(90); // ทดสอบเล่นเสียงเริ่มต้นระบบ
  }

  // 2. เริ่มต้นระบบบลูทูธ BLE
  BLEDevice::init("SafeSight-SmartHelmet");
  pServer = BLEDevice::createServer();
  pServer->setCallbacks(new MyServerCallbacks());

  BLEService *pService = pServer->createService(SERVICE_UUID);

  pTxCharacteristic = pService->createCharacteristic(
    CHARACTERISTIC_UUID_TX,
    BLECharacteristic::PROPERTY_NOTIFY
  );
  pTxCharacteristic->addDescriptor(new BLE2902());

  BLECharacteristic *pRxCharacteristic = pService->createCharacteristic(
    CHARACTERISTIC_UUID_RX,
    BLECharacteristic::PROPERTY_WRITE | BLECharacteristic::PROPERTY_WRITE_NR
  );
  pRxCharacteristic->setCallbacks(new MyCallbacks());

  pService->start();
  pServer->getAdvertising()->start();
  Serial.println("📡 บลูทูธ BLE เริ่มกระจายสัญญาณในชื่อ 'SafeSight-SmartHelmet'");
}

void loop() {
  // หากมีเซนเซอร์อัลตราโซนิกตรวจจับสิ่งกีดขวาง สามารถส่งข้อมูลกลับมือถือผ่าน pTxCharacteristic ได้ที่นี่
  delay(50);
}
```
